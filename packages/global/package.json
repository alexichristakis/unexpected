{
  "name": "@unexpected/global",
  "version": "0.0.3",
  "scripts": {
    "build": "yarn && tsc",
    "clean": "rm -rf node_modules && rm -rf dist",
    "deploy": "yarn clean && yarn build && node scripts/deploy.js"
  },
  "dependencies": {
    "@tsed/common": "^5.53.1",
    "@tsed/mongoose": "^5.53.1"
  }
}
